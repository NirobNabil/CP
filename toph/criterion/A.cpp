#include "bits/stdc++.h"
using namespace std;
#define ll long long int

int main(){
	int t;
	scanf("%d", &t);
	while(t--){
		int n, s;
		scanf("%d %d", &n, &s);
		int a[n];
		for(int i=0; i<n; i++) scanf("%d", &a[i]);
	}
}
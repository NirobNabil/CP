#include "bits/stdc++.h"
using namespace std;
#define ll long long int

char a[100009];

int main(){
	int t, n, x;
	scanf("%d", &t);
	while(t--){
		x=0;
		stack<bool> g;
		scanf("%d", &n);
		scanf("%s", a);
		for(int i=1; i<n; i++) {
			if()
			g.push(a[i]-'0');
		}
	}
}
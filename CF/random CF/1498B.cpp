#include <bits/stdc++.h>
using namespace std;
#define ll long long int
#define pb(x) push_back(x)
#define X first
#define Y second
#define A begin
#define B end
#define S1 100000

int visited[S1];
void dfs( int n ) {
	
}

int main(){
	int i, ix, t, n, x, y, z;
	scanf("%d", &t);
	while(t--){
		scanf("%d", &n);
		vector<int> a(n);
		for(int i=0; i<n; i++) {
        scanf("%d", &i);
    }
    
	}
}
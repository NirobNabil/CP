#include <bits/stdc++.h>
using namespace std;
#define ll long long int

int grid[300][300];

int main(){
	int t, m, r;
	scanf("%d", &t);
	while(t--){
		scanf("%d %d", &m, &r);
		for(int i=0; i<m; i++) for(int ix=0; ix<n; ix++) scanf("%d", &grid[m][r]);
		int zeroes=0, nonzeroes=0;
		for(int i=0; i<m; i++) for(int ix=0; ix<n; ix++) ;
		
	}
}
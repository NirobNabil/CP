#include <bits/stdc++.h>
using namespace std;
#define ll long long int
#define pb(x) push_back(x)
#define X first
#define Y second
#define A begin
#define B end
#define S1 100000

int chk(int i) {
	
}

int main(){
	int i, ix, t, n, x, y, z;
	scanf("%d", &t);
	while(t--){
		scanf("%d", &n);
		vector<string> a(n);
		for(int i=0; i<n; i++) {
      cin >> a[i];
    }
    sort(a.A(), a.B());

	}
}
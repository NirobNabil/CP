#include <bits/stdc++.h>
using namespace std;
#define ll long long int
#define vv vector<int>
#define pp pair<int,int>

#define pb(x) push_back(x)
#define in(x) insert(x)
#define del(x) erase(x)
#define emp(x) emplace_back(x)
#define mt make_tuple

#define X first
#define Y second
#define A begin
#define B end
#define S1 100000



int main(){
	ll i, ix, t, n, m, k, p, q, gg, x, y, z, a=0, b=0;
	for(int i=0; i<5; i++) scanf("%d", &x), a+=x;
	for(int i=0; i<5; i++) scanf("%d", &x), b+=x;
	if(a >= b) printf("Blue\n");
	else printf("Red\n");
}
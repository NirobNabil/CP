#include "bits/stdc++.h"
using namespace std;
#define ll long long int

int main(){
	int t;
	scanf("%d", &t);
	char gg[15];
	sprintf(gg, "%d", t);
	for(int i=0; i<n; i++) {
		if(gg[i]!=gg[n-i]) {
			printf("%s\n", );
		}
	}
}